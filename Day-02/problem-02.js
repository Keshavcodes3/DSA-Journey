/*
Problem 2 — Switch-case practice (Question only)

Question: Implement a function `animalSound(animal)` that uses a switch
statement to return the typical sound for the provided animal string.

Requirements:
- If `animal` is 'dog' -> return 'woof'
- If `animal` is 'cat' -> return 'meow'
- If `animal` is 'cow' -> return 'moo'
- If `animal` is 'sheep' -> return 'baa'
- For any other input -> return 'unknown'

Do NOT include an implementation or solution in this file—only the problem
description is required here.
*/

// (Question-only file)
