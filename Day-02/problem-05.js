/*
Problem 5 — Switch-case practice (Question only)

Question: Implement a function `httpStatusMessage(code)` that uses a switch
statement to return a short message for common HTTP status codes.

Example mapping:
- 200 -> 'OK'
- 201 -> 'Created'
- 400 -> 'Bad Request'
- 401 -> 'Unauthorized'
- 404 -> 'Not Found'
- 500 -> 'Internal Server Error'

Requirements:
- Accept numeric `code` values and return the corresponding message.
- For unknown codes, return 'Unknown Status'.

Do NOT include an implementation here — this file should contain only the
problem description.
*/

// (Question-only file)

let ch='S';
ch.toLowerCase